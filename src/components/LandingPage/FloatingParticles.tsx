import React from 'react';

// Retired floating particles to prevent chaotic visual artifacts and maintain clean design standards.
const FloatingParticles: React.FC = () => {
	return null;
};

export default FloatingParticles;
